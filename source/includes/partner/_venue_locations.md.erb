## Room Rentals

This integration allows users to search MicroTek’s nearby venues and return results.
    
### Find a nearby venue

> Request:

```shell
curl <%= config[:api_host] %>/v1/partner/room-rentals/find-nearby-venues
     -X POST
     -d '{
            "query": "chicago",
            "radius": 60
         }'
     -H 'content-type: application/json'
     -H 'authorization: Bearer your-account-token'
```

```csharp
var client = new RestClient("<%= config[:api_host] %>/v1/partner/room-rental/find-nearby-venue");
var request = new RestRequest(Method.POST);
request.AddHeader("content-type", "application/json");
request.AddHeader("authorization", "Bearer {token}");
request.AddParameter("courseKey", "00000000-0000-0000-0000-000000000000"", ParameterType.RequestBody);
IRestResponse response = client.Execute(request);
```

> Response:

```
200 OK
```

```json
{
    "type": "Geographic Distance",
    "query": "chicago",
    "radius": 30,
    "geographic_areas": [
      {
        "city": "Chicago",
        "region": "IL",
        "country": "US",
        "latitude": 41.899078333333335,
        "longitude": -87.664288,
        "venues": [
          {
            "id": 50026,
            "name": "MicroTek - Chicago",
            "latitude": 41.881,
            "longitude": -87.634704
          }
        ]
      }
    ]
  }
  
```
<api>`GET /v1/partner/venue-locations?query=chicago`</api>

Performs a facility search around a geographic area

Parameter | Type | Description
--------- | ---- | -----------
query **required** | *string* | A geopgrahic area such as a city, state or country
radius **optional** | *int* | Search radius in miles.  Defaults to 30 miles.

