### Get

> Request:

```shell
curl <%= config[:api_host] %>/v1/customer/course/get
     -X POST
     -d '{
            "courseKey": "00000000-0000-0000-0000-000000000000"
        }'
     -H 'content-type: application/json'
     -H 'authorization: Bearer your-account-token'
```

```csharp
var client = new RestClient("<%= config[:api_host] %>/v1/customer/course/get");
var request = new RestRequest(Method.POST);
request.AddHeader("content-type", "application/json");
request.AddHeader("authorization", "Bearer {token}");
request.AddParameter("courseKey", "00000000-0000-0000-0000-000000000000"", ParameterType.RequestBody);
IRestResponse response = client.Execute(request);
```

> Response:

```
200 OK
```

```json
{
    "result": {
        "courseKey": "00000000-0000-0000-0000-000000000000",
        "courseName": "Sample Title",
        "courseCode": null,
        "description": null,
        "isActive": true,
        "supportsTrainingRoom": true,
        "supportsVirtualTrainingLab": true
    },
    "status": "Ok"
}
```
<api>`POST /v1/customer/course/get`</api>

Returns a single course.

#### Attributes
Parameter | Type | Description
--------- | ---- | -----------
courseKey **required** | *guid* | Key

