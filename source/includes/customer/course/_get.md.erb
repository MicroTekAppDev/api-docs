### Get a single course

> Request:

```shell
curl -X GET \
  <%= config[:api_host] %>/v1/customer/courses/:course_key \
  -H 'authorization: Bearer your-account-token' \
  -H 'content-type: application/json' \
  -H 'cache-control: no-cache'
```

```csharp
var client = new RestClient("<%= config[:api_host] %>/v1");
var request = new RestRequest("customer/courses/:course_key", Method.GET);
request.AddHeader("authorization", "Bearer {token}");
request.AddHeader("content-type", "application/json");
request.AddHeader("cache-control", "no-cache");
var response = client.Execute(request);
```

> Response:

```
200 OK
```

```json
{
  "course_key": "00000000-0000-0000-0000-000000000000",
  "course_name": "Acme 101",
  "course_code": null,
  "description": null,
  "is_active": true,
  "room_rental_ordering_enabled": true,
  "virtual_lab_ordering_enabled": true
}
```
<api>`GET /v1/customer/courses/:course_key`</api>

Returns a single course.

Parameter | Type | Description
--------- | ---- | -----------
course_key **required** | *guid* | Key