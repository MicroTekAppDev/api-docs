### List courses

> Request:

```shell
curl -X GET \
  <%= config[:api_host] %>/v1/customer/courses \
  -H 'authorization: Bearer your-account-token' \
  -H 'content-type: application/json' \
  -H 'cache-control: no-cache'
```

```csharp
var client = new RestClient("<%= config[:api_host] %>/v1");
var request = new RestRequest("customer/courses", Method.GET);
request.AddHeader("authorization", "Bearer {token}");
request.AddHeader("content-type", "application/json");
request.AddHeader("cache-control", "no-cache");
var response = client.Execute(request);
```

> Response:

```
Status: 200 OK
```

```json
{
  "page": 1,
  "page_size": 1,
  "page_records": 1,
  "total_pages": 7,
  "total_records": 7,
  "results":
  [
    {
      "course_key": "68ce00f1-7d2c-4cbc-b401-2f419a7caf53",
      "course_name": "Centrify Advanced Track",
      "course_code": null,
      "description": null,
      "is_active": true,
      "room_rental_ordering_enabled": true,
      "virtual_lab_ordering_enabled": true
    }
  ]
}
```

<api>`GET /v1/customer/courses`</api>

Returns a list of courses.

Parameter | Type | Description
--------- | ---- | -----------
Pagination *optional* | | This endpoint can be [paginated](#api-reference-pagination).