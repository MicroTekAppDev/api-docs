### Get a single order request

> Request:

```shell
curl <%= config[:api_host] %>/v1/customer/order-requests/:requestKey
     -X GET
     -H 'authorization: Bearer your-account-token'
```
> Response:

```
Status: 200 OK
```

```json
{
}
```
<api>`GET /v1/customer/order-requests/:requestKey`</api>

Returns a single order request.

Parameter | Type | Description
--------- | ---- | -----------
requestKey **required** | *guid* | Key