### Cancel an order request

> Request:

```shell
curl -X POST \
  <%= config[:api_host] %>/v1/customer/order-requests/:request_key/cancel \
  -H 'authorization: Bearer your-account-token'
  -H 'cache-control: no-cache' \
  -H 'content-type: application/json' \
```
> Response:

```
Status: 204 No Content
```
<api>`POST /v1/customer/order-requests/:request_key/cancel`</api>

Cancels an order request

Parameter | Type | Description
--------- | ---- | -----------
request_key **required** | *guid* | Key