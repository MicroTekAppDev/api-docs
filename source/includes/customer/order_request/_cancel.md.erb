### Cancel

> Request:

```shell
curl <%= config[:api_host] %>/v1/customer/order/request/cancel
     -X POST
     -d '{
            "requestKey": "00000000-0000-0000-0000-000000000000"
        }'
     -H 'content-type: application/json'
     -H 'authorization: Bearer your-account-token'
```
> Response:

```
200 OK
```

```json
{
    "status": "Ok"
}
```
<api>`POST /v1/customer/order/request/cancel`</api>

Cancels an order request

#### Attributes
Parameter | Type | Description
--------- | ---- | -----------
requestKey **required** | *guid* | Key
