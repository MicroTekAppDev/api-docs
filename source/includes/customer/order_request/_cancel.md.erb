### Cancel an order request

> Request:

```shell
curl <%= config[:api_host] %>/v1/customer/order-requests/:requestKey/cancel
     -X POST
     -H 'authorization: Bearer your-account-token'
```
> Response:

```
Status: 204 No Content
```
<api>`POST /v1/customer/order-requests/:requestKey/cancel`</api>

Cancels an order request

Parameter | Type | Description
--------- | ---- | -----------
requestKey **required** | *guid* | Key